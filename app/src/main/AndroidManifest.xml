<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Renderização Vulkan (renderer nativo do rexglue; VK_KHR_android_surface) -->
    <uses-feature
        android:name="android.hardware.vulkan.version"
        android:version="0x400003"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.vulkan.compute"
        android:version="0"
        android:required="false" />

    <!-- Gamepads Bluetooth HID (SDL3 HIDDeviceManager); USB não exige permissão -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />

    <!--
      Fluxo de pasta SEM CÓPIA: o motor (librestuff.so) abre os arquivos do
      jogo direto do armazenamento do usuário via POSIX (game_data_root é o
      caminho real). Isso exige "Acesso a todos os arquivos" (Android 11+,
      concedido via Settings — igual aos emuladores/ports). Em Android 9/10 o
      acesso direto a caminhos usa READ_EXTERNAL_STORAGE +
      requestLegacyExternalStorage.
    -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        android:minSdkVersion="30" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PortScreen">

        <!-- Launcher / tela de seleção de dados do port -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|density"
            android:theme="@style/Theme.PortScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            GameActivity: hospeda a janela SDL3 (org.libsdl.app.SDLActivity) que
            roda o binário nativo do recomp (librestuff.so). Travada em landscape
            (o jogo é 16:9) e sem recreação em resize/teclado — o ciclo SDL não
            deve ser destruído por configChanges.
        -->
        <activity
            android:name=".game.GameActivity"
            android:exported="false"
            android:screenOrientation="sensorLandscape"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|uiMode|density|navigation"
            android:hardwareAccelerated="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.PortScreen" />
    </application>

</manifest>
